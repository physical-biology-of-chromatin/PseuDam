profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $dedup_sam {
        container = "samblaster:0.1.24"
      }
    }
  }
  sge {
    process{
      $dedup_sam {
        beforeScript = "module purge; module load samblaster/0.1.24"
      }
    }
  }
}
