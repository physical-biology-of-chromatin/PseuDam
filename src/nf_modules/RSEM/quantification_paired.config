profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $mapping_fastq {
        container = "rsem:1.3.0"
      }
    }
  }
  sge {
    process{
      $mapping_fastq {
        beforeScript = "module purge; module load RSEM/1.3.0; module load SAMtools/1.7"
      }
    }
  }
}
