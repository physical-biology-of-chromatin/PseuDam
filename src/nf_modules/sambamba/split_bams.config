profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $split_bam {
        container = "sambamba:0.6.7"
      }
    }
  }
  sge {
    process{
      $split_bam {
        beforeScript = "module purge; module load sambamba/0.6.7"
      }
    }
  }
}
