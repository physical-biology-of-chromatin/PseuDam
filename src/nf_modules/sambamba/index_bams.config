profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $index_bam {
        container = "sambamba:0.6.7"
      }
    }
  }
  psmn {
    process{
      $index_bam {
        beforeScript = "module purge; module load sambamba/0.6.7"
      }
    }
  }
}
