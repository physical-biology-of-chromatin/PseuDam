profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $sort_bam {
        container = "samtools:1.7"
      }
    }
  }
  psmn {
    process{
      $sort_bam {
        beforeScript = "module purge; module load SAMtools/1.7"
      }
    }
  }
}
