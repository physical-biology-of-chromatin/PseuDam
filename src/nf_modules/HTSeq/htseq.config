profiles {
  docker {
    docker.temp = 'auto'
    docker.enabled = true
    process {
      $counting {
        container = "htseq:0.8.0"
      }
    }
  }
  sge {
    process{
      $trimming {
        beforeScript = "module purge; module load HTSeq/0.8.0"
      }
    }
  }
}
